# HTTPS SSL Project

This project focuses on securing web traffic with HTTPS and configuring SSL termination using HAProxy.

## Files

### 0-world_wide_web
A Bash script that checks IPv4 addresses of common subdomains for bonasse.site.

### 1-haproxy_ssl_termination
Installs HAProxy, generates a self-signed SSL certificate, and sets up SSL termination.

